{
  "name": "@doc-ai/vector-ingestion-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "VECTOR_INGESTION_AGENT_PORT=${VECTOR_INGESTION_AGENT_PORT:-3305} VECTOR_MCP_URL=${VECTOR_MCP_URL:-http://localhost:3403} tsx watch src/server.ts"
  },
  "dependencies": {
    "@doc-ai/api-contracts": "workspace:*",
    "@doc-ai/agent-runtime": "workspace:*",
    "@doc-ai/shared-config": "workspace:*",
    "uuid": "^11.0.5"
  }
}
